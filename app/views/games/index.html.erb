
<div class="game-library">
  <div class="container py-5">
    <div class="text-center mb-5">
      <h1 class="display-4 fw-bold" style="color: white;">🎮 Game Library</h1>
      <p class="lead" style="color: rgba(255, 255, 255, 0.7);">Ваша коллекция лучших игр</p>
    </div>

    <div class="add-game">
      <%= link_to new_game_path, class: "btn btn-success btn-lg shadow-sm", style: "background-color: #2d6b2a; border: none;" do %>
        <i class="bi bi-plus-circle me-2"></i> Добавить игру
      <% end %>
    </div>

    <div class="games-container">
      <% @games.each do |game| %>
        <div class="game-card">
          <div class="card-header" style="background-color: #3e8e41; color: white;">
            <h3><%= game.title %></h3>
          </div>
          <div class="card-body">
            <div class="mb-3">
              <span class="badge" style="background-color: #0d6efd;"><%= game.genre %></span>
              <span class="badge" style="background-color: #6c757d; margin-left: 0.5rem;"><%= game.platform %></span>
            </div>
            <p class="card-text">
              <i class="bi bi-people-fill text-muted"></i> <%= game.developer %>
            </p>
            <p class="date">
              <i class="bi bi-calendar-event text-muted"></i> <%= game.release_date %>
            </p>
          </div>
          <div class="actions">
            <%= link_to game, class: "btn btn-sm" do %>
              <i class="bi bi-eye me-1"></i> Подробнее
            <% end %>
            <%= link_to edit_game_path(game), class: "btn btn-sm" do %>
              <i class="bi bi-pencil me-1"></i> Редактировать
            <% end %><br><br>
            <%= link_to game_path(game), 
                  method: :delete, 
                  data: { turbo_method: :delete, turbo_confirm: "Вы уверены, что хотите удалить игру?" },
                  class: "btn btn-sm" do %>
              <i class="bi bi-trash me-1"></i> Удалить
            <% end %>
          </div>
        </div>
      <% end %>
    </div>

    <% if @games.empty? %>
      <div class="text-center py-5">
        <div class="alert alert-info" style="background-color: rgba(255, 255, 255, 0.2); border: none; color: white;">
          <i class="bi bi-info-circle-fill me-2"></i> Ваша коллекция игр пуста. Добавьте первую игру!
        </div>
      </div>
    <% end %>

    <div class="d-flex justify-content-center mt-5">
      <%= will_paginate @games, 
            theme: 'bootstrap5',
            previous_label: '<i class="bi bi-chevron-left"></i>',
            next_label: '<i class="bi bi-chevron-right"></i>' %>
    </div>
  </div>

  
</div>